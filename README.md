# Intelligent Resume Analyzer

A pure Python (no external libraries) tool that ranks resumes against a Job Description using TF‑IDF and cosine similarity.

## Features
- Reads `.txt` and `.docx` files.
- No internet required.
- Simple GUI built with Tkinter.
- Exports results to CSV.

## How to run
```bash
python main.py
